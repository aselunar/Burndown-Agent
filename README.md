# Burndown-Agent
An agent you can plugin to RooCode to connect to your backlog and GitHub repository to have the agent burndown your backlog in priority order by creating PRs in your GitHub repo


curl -sL http://host.docker.internal:8000/install.sh | sh
